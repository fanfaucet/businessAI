# Simulation

Purpose: tabletop simulation framework for synthetic events such as port closures, fuel shocks, healthcare crises, and supply-chain disruptions.

Architecture: scenarios are deterministic inputs that produce cascading delay forecasts, saturation metrics, resilience scores, and confidence values. Simulations are explicitly marked simulation-only and cannot be promoted to operational commands.
